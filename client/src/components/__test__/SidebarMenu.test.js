import { render, screen } from "@testing-library/react";

test("Render the unauthenticated sidebar menu", async () => {
  // render(<MockInvitationCardProvider />);
  // const InvitationCardElement = screen.getByAltText();
  expect(1 + 1).toEqual(2);
});
