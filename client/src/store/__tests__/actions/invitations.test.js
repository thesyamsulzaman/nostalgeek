import "regenerator-runtime";

describe("actions/invitations", () => {
  it("createInvitation", () => {
    expect(1).toBe(1);
  });

  it("deleteInvitation", () => {
    expect(1).toBe(1);
  });
});
